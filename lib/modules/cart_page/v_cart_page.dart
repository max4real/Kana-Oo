import 'package:flutter/material.dart';

class InCart extends StatelessWidget {
  const InCart({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("in cart"),
      ),
    );
  }
}